[project]
name = "optionslab-pro"
version = "0.1.0"
description = "Yield Engine — options trading database and analytics"
requires-python = ">=3.11"

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "--tb=short"

[tool.coverage.run]
source = ["db"]
omit = ["tests/*"]
